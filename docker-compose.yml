version: "3.9"

services:
  backend:
    build:
      context: ./backend

    container_name: medical-rag-backend

    ports:
      - "8000:8000"

    env_file:
      - ./backend/.env

    volumes:
      - ./backend/data:/app/data

    restart: unless-stopped